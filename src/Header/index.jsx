import React from 'react';
import { Button } from 'antd';

function Header() {
  return (
    <div>
      <Button type="primary">按钮</Button>
    </div>
  );
}

Header.propTypes = {

};

export default Header;

